<h2><%= item.name %> Details</h2>
<div class="tortoise-images">
  <% if item.images.attached? %>
    <% item.images.each do |image| %>
      <%= image_tag image, class: "img-fluid mb-2" %>
    <% end %>
  <% else %>
    <p>No images available.</p>
  <% end %>
</div>
<p><strong>Species:</strong> <%= item.species %></p>
<p><strong>Price:</strong> <%= number_to_currency(item.price) %></p>
<p><strong>Quantity:</strong> <%= item.quantity %></p>
<p><strong>Total:</strong> <%= number_to_currency(item.quantity * item.price) %></p>
<%= link_to "Back to Cart", cart_path, class: "btn btn-secondary" %>