<% if following %>
   <%= button_to 'Unfollow', unfollow_path(profile.id), class: "btn btn-danger btn-sm mt-1 ms-2", remote: true, method: :delete %>
<% else %>
  <%= button_to 'Follow', follow_path(profile.id), class: "btn btn-primary btn-sm mt-1 ms-2", remote: true, method: :post %>
<% end %>
